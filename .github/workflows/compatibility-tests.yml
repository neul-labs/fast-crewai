name: CrewAI Compatibility Tests

on:
  # Run on pull requests
  pull_request:
    branches: [ main, develop ]

  # Run on pushes to main
  push:
    branches: [ main ]

  # Allow manual trigger
  workflow_dispatch:

  # Run weekly to catch CrewAI changes
  schedule:
    - cron: '0 0 * * 0'  # Every Sunday at midnight

jobs:
  compatibility-test:
    name: Test with CrewAI ${{ matrix.crewai-branch }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ['3.10', '3.11', '3.12', '3.13']
        crewai-branch: ['main']

    steps:
    - name: Checkout Fast-CrewAI
      uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true
        cache-dependency-glob: "pyproject.toml"

    - name: Set up Python ${{ matrix.python-version }}
      run: uv python install ${{ matrix.python-version }}

    - name: Set up Rust
      uses: actions-rust-lang/setup-rust-toolchain@v1
      with:
        toolchain: stable

    - name: Cache Rust build
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential

    - name: Install dependencies
      run: uv sync --dev

    - name: Run Compatibility Tests
      env:
        CREWAI_BRANCH: ${{ matrix.crewai-branch }}
      run: |
        # Run tests that don't require OPENAI_API_KEY
        # These test utility functions, serialization, event handling, etc.
        uv run python scripts/test_crewai_compatibility.py \
          --crewai-branch "$CREWAI_BRANCH" \
          --keep-env \
          --verbose \
          --no-api-key

    - name: Upload compatibility report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: compatibility-report-py${{ matrix.python-version }}-${{ matrix.crewai-branch }}
        path: test_compatibility/COMPATIBILITY.md
        retention-days: 30

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-${{ matrix.os }}-py${{ matrix.python-version }}-${{ matrix.crewai-branch }}
        path: |
          test_compatibility/pytest_results.json
          test_compatibility/COMPATIBILITY.md
        retention-days: 30

    - name: Comment on PR (on failure)
      if: failure() && github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: '⚠️ CrewAI compatibility tests failed. Please review the test results.'
          })

  compatibility-test-summary:
    name: Compatibility Test Summary
    needs: compatibility-test
    runs-on: ubuntu-latest
    if: always()

    steps:
    - name: Check test results
      run: |
        if [ "${{ needs.compatibility-test.result }}" == "success" ]; then
          echo "✅ All compatibility tests passed!"
          exit 0
        else
          echo "❌ Some compatibility tests failed"
          exit 1
        fi
